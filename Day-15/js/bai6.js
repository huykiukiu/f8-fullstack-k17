/* Cho 2 biến username và password.
Dùng toán tử đã học để kiểm tra xem username và password khác rỗng không 
(In ra giá trị boolean). */

let username = "huy";
let password = "";
console.log(username !== "" && password !== "");
