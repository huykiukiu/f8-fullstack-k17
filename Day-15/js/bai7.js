/* Cho trước giá khuyến mãi (salePrice), giá gốc (regularPrice). Tính phần trăm giảm giá của sản phẩm. 

*/

let salePrice = 20000;
let regularPrice = 100000;
let discount = ((regularPrice - salePrice) / regularPrice) * 100;
console.log(discount);
